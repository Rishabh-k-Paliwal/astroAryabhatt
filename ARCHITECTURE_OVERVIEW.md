# 📊 SYSTEM ARCHITECTURE & DEPLOYMENT OVERVIEW

## 🏗️ COMPLETE SYSTEM ARCHITECTURE

```
┌─────────────────────────────────────────────────────────────────────┐
│                         USER BROWSER                                 │
│                  http://localhost:5174                               │
└──────────────────────────┬──────────────────────────────────────────┘
                           │ (React App - Vite)
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
        ▼                  ▼                  ▼
    Pages/         Components/         Services/
    ├─ Home        ├─ Header           ├─ api.js
    ├─ About       ├─ BookingForm      └─ razorpay.js
    ├─ Services    ├─ DatePicker
    ├─ Book        └─ SlotPicker
    └─ Success

        │
        │ HTTPS API Calls
        │
┌───────▼──────────────────────────────────────────────────────────────┐
│                      NODE.JS BACKEND                                 │
│                    http://localhost:5000                             │
│  ┌─────────────────────────────────────────────────────────┐        │
│  │               Express Server (app.js)                   │        │
│  ├─────────────────────────────────────────────────────────┤        │
│  │  Routes:                                                │        │
│  │  ├─ /api/slots          (Slot Management)             │        │
│  │  ├─ /api/bookings       (Booking Operations)          │        │
│  │  ├─ /api/payment        (Razorpay Integration)        │        │
│  │  └─ /api/admin          (Admin Dashboard APIs)        │        │
│  │                                                         │        │
│  │  Middleware:                                            │        │
│  │  ├─ errorHandler.js     (Error Handling)              │        │
│  │  ├─ verifyPayment.js    (Payment Verification)        │        │
│  │  └─ verifyWebhook.js    (Webhook Verification)        │        │
│  │                                                         │        │
│  │  Services:                                              │        │
│  │  ├─ email.service.js    (Nodemailer)                  │        │
│  │  ├─ googleMeet.service  (Meet Link Generation)        │        │
│  │  └─ whatsapp.service.js (Optional WhatsApp)           │        │
│  └─────────────────────────────────────────────────────────┘        │
└───────┬──────────────┬──────────────┬─────────────────────────────┬──┘
        │              │              │                             │
        ▼              ▼              ▼                             ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│  MONGODB     │  │  RAZORPAY    │  │   NODEMAILER │  │ GOOGLE MEET  │
│              │  │              │  │    + Gmail   │  │              │
│ Collections: │  │ - Test Mode  │  │ - Send Emails│  │ - API Token  │
│ • bookings   │  │ - Webhooks   │  │ - Templates  │  │ - Create Link│
│ • slots      │  │ - Payments   │  │ - Tracking   │  │ - Guests     │
│ • payments   │  │ - Refunds    │  │ - Scheduled  │  │ - Recording  │
└──────────────┘  └──────────────┘  └──────────────┘  └──────────────┘
```

---

## 🔄 BOOKING PROCESS FLOW

```
┌─────────────────────────────────────────────────────────────────────┐
│                        START: User on Home                           │
└────────────────────────────┬────────────────────────────────────────┘
                             │
                             ▼
                   ┌──────────────────────┐
                   │  Navigate to /book   │
                   └──────────┬───────────┘
                              │
                              ▼
              ┌───────────────────────────────────┐
              │   Frontend: Book Page Component   │
              │   - Show calendar                 │
              │   - Load available slots          │
              └──────────────┬────────────────────┘
                             │
             ┌───────────────┴──────────────┐
             │                              │
             ▼                              ▼
    ┌──────────────────┐        ┌──────────────────┐
    │ User Selects     │        │ API Call:        │
    │ Date from        │        │ GET /api/slots   │
    │ Calendar         │        │                  │
    └──────────┬───────┘        └────────┬─────────┘
               │                         │
               ▼                         ▼
    ┌──────────────────────────────────────────────┐
    │ Backend:                                     │
    │ 1. Query MongoDB for available slots         │
    │ 2. Filter by date                            │
    │ 3. Return slots that aren't booked           │
    └───────────┬────────────────────────────────┘
                │
                ▼
    ┌──────────────────────────────────┐
    │ Frontend: Display Time Slots      │
    │ - Show available times            │
    │ - User clicks on preferred slot   │
    └───────────┬──────────────────────┘
                │
                ▼
    ┌──────────────────────────────────┐
    │ Frontend: Show Form               │
    │ - Name, Email, Phone              │
    │ - Birth Date, Time, Location      │
    │ - Service Type                    │
    └───────────┬──────────────────────┘
                │
                ▼
    ┌──────────────────────────────────┐
    │ User Submits Form                 │
    │ API Call: POST /api/bookings      │
    └───────────┬──────────────────────┘
                │
                ▼
    ┌────────────────────────────────────────────┐
    │ Backend:                                   │
    │ 1. Validate form data                      │
    │ 2. Create booking document (status:PENDING)│
    │ 3. Save to MongoDB                         │
    │ 4. Create Razorpay order                   │
    └───────────┬────────────────────────────────┘
                │
                ▼
    ┌────────────────────────────────────────────┐
    │ Frontend: Razorpay Payment Modal           │
    │ - User enters test card details             │
    │ - Card: 4111111111111111                    │
    │ - Expiry: 12/25, CVV: 123                   │
    └───────────┬────────────────────────────────┘
                │
                ▼
    ┌────────────────────────────────────────────┐
    │ Razorpay: Process Payment                  │
    │ - Validate card                             │
    │ - Charge amount (₹999)                      │
    │ - Generate payment ID                       │
    └───────────┬────────────────────────────────┘
                │
         ┌──────┴──────┐
         │             │
    ✅ SUCCESS    ❌ FAILED
         │             │
         ▼             ▼
    ┌────────┐   ┌──────────────┐
    │Webhook │   │Return Error  │
    │Fired   │   │to Frontend   │
    └────┬───┘   └──────────────┘
         │
         ▼
    ┌──────────────────────────────────┐
    │ Backend: Webhook Handler         │
    │ 1. Verify webhook signature      │
    │ 2. Get payment details           │
    │ 3. Update booking:               │
    │    - status: CONFIRMED           │
    │    - paymentId: set              │
    │ 4. Mark slot as booked           │
    │ 5. Generate Google Meet link     │
    │ 6. Send confirmation email       │
    │ 7. Return success to frontend    │
    └───────────┬──────────────────────┘
                │
                ▼
    ┌──────────────────────────────────┐
    │ Frontend: Success Page           │
    │ - Show booking ID                │
    │ - Show consultation details      │
    │ - Countdown to consultation      │
    │ - "Go Home" button               │
    └──────────────────────────────────┘

PARALLEL PROCESSES:
    │
    ├─→ Nodemailer sends confirmation email
    ├─→ Database updated with booking
    ├─→ Slot marked as unavailable
    ├─→ Payment recorded in payments collection
    └─→ Scheduler set for reminders (24hrs before)
```

---

## 📦 DEPLOYMENT ARCHITECTURE OPTIONS

### Option 1: ⚡ RECOMMENDED - Vercel + Railway

```
┌─────────────────────────────────────────────────┐
│           PRODUCTION ENVIRONMENT                 │
├─────────────────────────────────────────────────┤
│                                                 │
│  Domain: yourdomain.com                         │
│  └─ DNS Points to Vercel                        │
│                                                 │
│  ┌────────────────────────────────────────────┐│
│  │  VERCEL (Frontend)                         ││
│  │  - React build deployed                    ││
│  │  - Auto-scaling                            ││
│  │  - CDN for static files                    ││
│  │  - SSL by default                          ││
│  │  Cost: $0-20/month                         ││
│  │  URL: yourdomain.com                       ││
│  └────────────────┬─────────────────────────┬─┘
│                   │                         │
│                   │ API Calls               │
│                   ▼                         │
│  ┌────────────────────────────────────────┐ │
│  │  RAILWAY (Backend)                     │ │
│  │  - Node.js/Express running             │ │
│  │  - Environment variables set           │ │
│  │  - Auto-restart on crash               │ │
│  │  - Logs accessible                     │ │
│  │  Cost: $5-25/month                     │ │
│  │  URL: api.yourdomain.com (or Railway)  │ │
│  └────────────┬───────────────────────────┘ │
│               │                              │
│  ┌────────────▼──────────────────────────┐ │
│  │  MONGODB ATLAS (Database)             │ │
│  │  - Cloud-hosted                       │ │
│  │  - Backups automatic                  │ │
│  │  - 512MB free tier                    │ │
│  │  Cost: $10-50/month                   │ │
│  │  URL: mongodb+srv://...               │ │
│  └────────────────────────────────────────┘ │
│                                              │
│  ┌────────────────────────────────────────┐ │
│  │  EXTERNAL SERVICES                     │ │
│  ├────────────────────────────────────────┤ │
│  │  • Razorpay: Payment Processing        │ │
│  │  • Gmail: Email Sending                │ │
│  │  • Google Meet: Video Calls            │ │
│  │  • Sentry: Error Monitoring (optional) │ │
│  │  • Cloudflare: DDoS Protection         │ │
│  └────────────────────────────────────────┘ │
│                                              │
└─────────────────────────────────────────────┘
```

### Option 2: 🐳 DOCKER + AWS

```
┌──────────────────────────────────────────────┐
│    AWS ARCHITECTURE                          │
├──────────────────────────────────────────────┤
│                                              │
│  Route 53 (DNS)                              │
│     │                                        │
│     ▼                                        │
│  CloudFront (CDN)                            │
│     │                                        │
│  ┌──┴───┐                                    │
│  │      │                                    │
│  ▼      ▼                                    │
│  ┌──────────────┐    ┌──────────────┐       │
│  │ S3 Bucket    │    │ ALB          │       │
│  │ (Frontend)   │    │ (Backend)    │       │
│  └──────────────┘    │              │       │
│                      │  ┌────┐     │       │
│                      │  │ECS │     │       │
│                      │  │Pod1│     │       │
│                      │  ├────┤     │       │
│                      │  │ECS │     │       │
│                      │  │Pod2│     │       │
│                      │  └────┘     │       │
│                      └──────────────┘       │
│                           │                 │
│                           ▼                 │
│                    ┌─────────────┐          │
│                    │  RDS/DynamoDB│ (DB)  │
│                    └─────────────┘         │
│                           │                 │
│                    Cost: $50-200/month     │
│                    Highly Scalable ✅      │
│                                            │
└──────────────────────────────────────────┘
```

---

## 🚀 QUICK DEPLOYMENT COMMAND

```bash
# Deploy Frontend (Vercel)
$ cd client
$ npm run build
$ vercel --prod

# Deploy Backend (Railway)
# Push to GitHub → Railway auto-deploys
$ git add .
$ git commit -m "Deploy production"
$ git push origin main

# Total time: 15 minutes ⚡
```

---

## 💡 KEY DECISION POINTS

| Decision | What to Choose | Why |
|----------|---|---|
| **Frontend Host** | Vercel | Fast, free tier, auto-scaling |
| **Backend Host** | Railway | Easy setup, affordable, great for Node |
| **Database** | MongoDB Atlas | No-SQL, flexible schema, free tier |
| **Payments** | Razorpay | India-focused, low fees, great support |
| **Emails** | Nodemailer + Gmail | Free, simple, reliable |
| **Monitoring** | Sentry | Error tracking, free tier good |

---

## ✅ PRODUCTION READINESS CHECKLIST

- ✅ Frontend fully responsive
- ✅ Backend API tested
- ✅ Database schema optimized
- ✅ Payment integration working
- ✅ Email notifications ready
- ✅ Error handling implemented
- ✅ Security best practices in place
- ✅ Documentation complete
- ✅ Test suite provided
- ✅ Deployment guides prepared

---

**Your system is production-ready. Choose your deployment option and go live!** 🚀
